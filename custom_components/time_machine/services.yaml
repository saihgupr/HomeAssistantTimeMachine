backup_now:
  name: Backup Now
  description: Triggers an immediate backup in Home Assistant Time Machine.
  fields:
    url:
      name: URL
      description: >
        Override the addon URL for this call (optional).
        Defaults to the URL configured in the integration.
      required: false
      example: "http://homeassistant-time-machine:54000"
      selector:
        text:
